{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_rest(form) }} {# Ajoute les champs restants, y compris _token #}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

